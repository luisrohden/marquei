{
  "name": "marquei",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "esbuild src/app.js --bundle --outfile=bundle.js --watch",
    "build": "esbuild src/app.js --bundle --minify --outfile=bundle.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
